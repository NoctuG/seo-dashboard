"""Integration providers package.

Import provider modules here so they self-register at runtime.
"""

from app.integrations.google_search_console import GoogleSearchConsoleProvider as GoogleSearchConsoleProvider
